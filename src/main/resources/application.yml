
server:
  port: 8080

spring:
  datasource:
    username: root
    password: neil
    url: jdbc:mysql://localhost:3306/book_keeping?useUnicode=true&characterEncoding=utf-8&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true

#showSql
logging:
  level:
    root: info
    com.bookkeeping: info

